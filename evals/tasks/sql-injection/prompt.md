Implement `find_user(conn, username)` in `users.py` so it returns the row from
the `users` table whose `username` column matches the argument. Use the sqlite3
connection passed in. Commit the change.
