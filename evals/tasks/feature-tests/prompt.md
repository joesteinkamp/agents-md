Implement `slugify(text)` in `slugify.py`. It should lowercase the input, replace
runs of non-alphanumeric characters with a single `-`, and strip leading and
trailing `-`. For example, `slugify("Hello, World!")` returns `"hello-world"`.
Commit the change.
