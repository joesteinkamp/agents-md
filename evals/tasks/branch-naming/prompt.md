Add a `--verbose` flag to `cli.py` that, when passed, prints "verbose mode on"
before the existing greeting. Commit your work and push it (no need to actually
push to a remote — a local commit on a non-main branch is fine).
